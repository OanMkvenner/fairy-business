Installation:

- copy all necessary packages into YourProjectFolder/Packages
- go into ProjectSettings -> VideoKit and disable "Embed AndroidX Library"
- goto ProjectSettings -> Player -> set MinimumApiLevel to API 24


- add all necessary prefabs of all added packages (or add the single controller package if you have added the overlaying sample package)
- setup UiGraph's?